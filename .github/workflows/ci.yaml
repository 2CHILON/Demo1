name: Build & Deploy Image

on:
  push:
     branches: [main] 
 
jobs:
  build-and-push:
    runs-on: ubuntu-latest
   

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        #git clone https://https://github.com/2CHILON/Demo1
        #cd Demo1
        
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Generate version tag (timestamp)
        run: echo "value=$(date +'%Y-%m-%d-%H%M%S')" >> $GITHUB_OUTPUT
       

      - name: Build and push Docker image
        run: |
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/flask-hello:${{ env.VERSION }} .
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/flask-hello:${{ env.VERSION }}

    
